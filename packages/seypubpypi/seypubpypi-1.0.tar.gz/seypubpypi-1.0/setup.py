import setuptools

setuptools.setup(name="seypubpypi",
                 version="1.0", 
                 long_description="",
                 packages=setuptools.find_packages(exclude=["data", "tests"]))