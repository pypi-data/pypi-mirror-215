[build-system]
requires = [
    "setuptools",
]
build-backend = 'setuptools.build_meta'
[project]
name = 'hellomcb'
version = "0.0.2"
# requires-python = ">=3.8"

[project.scripts]
doothehello = 'hellomcb.somehello:doo_hello'

