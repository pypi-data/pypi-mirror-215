[build-system]
requires = ["setuptools", "setuptools-git-versioning"]
build-backend = "setuptools.build_meta"

[project]
name = "xrd-xy-parser"
description = "parse xy format file of X-ray diffraction"
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = ["numpy"]
dynamic = ["version"]

[project.urls]
"Homepage" = "https://gitlab.com/oksongh/xrd-xy-parser"

[tool.setuptools-git-versioning]
enabled = true

# authers = ["oksongh"]
