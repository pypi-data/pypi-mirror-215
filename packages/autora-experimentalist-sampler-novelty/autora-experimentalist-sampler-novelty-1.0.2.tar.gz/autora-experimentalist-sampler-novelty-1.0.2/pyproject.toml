[project]
# UPDATE THIS BEFORE PUBLISHING
name = "autora-experimentalist-sampler-novelty"
description = "AutoRA Novelty Experimentalist"
authors = [{ name = "Sebastian Musslick", email = "sebastian@musslick.de" }]
dynamic = ["version"]

readme = "README.md"
license = { text = "MIT License" }
requires-python = ">=3.8.10,<4"

# ADD NEW DEPENDENCIES HERE
dependencies = [
    "autora-core",
]

[project.optional-dependencies]
dev = [
    "autora-core[dev]"
]

[project.urls]
homepage = "http://www.empiricalresearch.ai"
repository = "https://github.com/AutoResearch/autora-experimentalist-sampler-novelty"
documentation = "https://autoresearch.github.io/autora/"

[build-system]
requires = ["setuptools", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
