[tool.poetry]
name = "hello-poetry"
version = "0.1.0"
description = "Hello, Poetry!"
authors = ["mancuoj <1337381768@qq.com>"]
readme = "README.md"
packages = [{ include = "hello_poetry" }]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = {extras = ["all"], version = "^0.98.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
