[tool.poetry]
name = "adp-py"
version = "0.0.1"
description = ""
authors = []
license = "MIT"
readme = "README.md"
packages = [{ include = "adp" }]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
