[tool.poetry]
name = "even-dist"
version = "0.0.2"
description = "A package to distribute points evenly."
authors = ["kirp <kirp@umich.edu>"]
license = "MIT"
readme = "README.md"
packages = [{include = "even_dist"}]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"