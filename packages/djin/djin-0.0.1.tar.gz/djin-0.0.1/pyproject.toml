[tool.poetry]
name = "djin"
version = "0.0.1"
description = "Context aware code assistent"
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
