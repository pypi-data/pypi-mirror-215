import logging

log = logging.getLogger('pebootloader')
# handler = logging.FileHandler('pebootloader.log')
# handler.setFormatter(logging.Formatter(logging.BASIC_FORMAT))
# log.addHandler(handler)
# log.setLevel(logging.INFO)
