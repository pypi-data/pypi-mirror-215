[tool.poetry]
name = "ana-sdk"
version = "0.3.18"
description = "SDK que visa fornecer uma interface para interagir com os serviços ANA."
authors = ["Jovane Mafort"]
license = "MIT License"
readme = "README.md"
packages = [{include = "ana_sdk"}]

[tool.poetry.dependencies]
python = "^3.10.12"
requests = "^2.27.1"
jsonschema = "^4.17.3"
python-dotenv = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
