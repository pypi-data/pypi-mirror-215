[tool.poetry]
name = "netbox-software"
version = "0.1.7"
description = ""
authors = ["izakharov <ilya.zakharov@domrf.ru>"]
readme = "README.md"
packages = [{include = "netbox_software"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
