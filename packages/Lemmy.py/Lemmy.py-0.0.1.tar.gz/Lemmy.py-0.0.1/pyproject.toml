[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "Lemmy.py"
version = "0.0.1"
dependencies = [
    "requests",
    'loguru; python_version>"3.10"',
]
